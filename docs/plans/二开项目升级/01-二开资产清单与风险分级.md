# 01 - 二开资产清单与风险分级

> 数据口径：`git diff upstream/main...HEAD`（merge-base 为 v2.1.40 时代上游合并点，diff 仅含二开侧改动）+ v2.1.52 隔离 worktree（`/private/tmp/aionui-latest-poc`，`git diff 7ebae30aa HEAD`）逐文件复核。
> 主分支二开增量（剔除浅克隆噪声后）：**64 个新文件、139 个修改文件中的 45 个源文件**（其余为 i18n locale 与测试），净增约 1.8 万行（含测试与翻译）。

## 0. 功能地图与提交溯源

| 功能 | 主分支来源 commit | v2.1.52 worktree 对应 |
| --- | --- | --- |
| 临时团队（ad-hoc team） | `700fdf117` 原型（溯源旧分支 `8b2953a33`）→ `8cbd1e833` 抽 adHocTeamBridge → `ea7757ce5` 回流收口 → `adf8dfaa4` 归属/门控收口；测试 `bee2093d9`/`35464bd9e`/`7f38664ae`/`a7efe3fb5`/`5c459df38`/`14d11cc6e` | `c0fbf26ae`/`a1f3e4b0b`（桥接适配 v0.1.62）、`c630ab397`、`1a7d1082c`、`058fbbc15`、`ff079002a`；**AdHocTeam 组件目录与回流/门控工具仍未提交** |
| 专家团（Team Presets） | `e3f154559` 落地 → `ebca50b35` 抽 teamPresetBridge；固化补丁 `patches/2026-07-30-*.patch` | `f5ab5f9b2`（Preset 管理面板）、`16cceab84`（团队头卡片）、`9990640d6`（恢复创建流）、`bd728fb4d`（i18n） |
| ACP 团队能力覆盖 | `63ffc91e9` | `a1f3e4b0b`（capability 类型对齐 v0.1.62） |
| 构建依赖对齐 | `cc9f05549`、`b19a68072`（builder-util-runtime 9.7.0 + resolutions） | 随基线自带，无需重做 |
| 本地 DB 迁移 | `700fdf117`（migration_v27） | 现行方案：迁移全部在 AionCore `crates/aionui-db/migrations/`（038/039/040） |
| 仓库卫生/文档 | `c1fdf8857`、`a494a25a6`、`b53df8d46`、`eb3bf7d8e`、`66e3b9c5f`、`272daa403`、`b397e15fd` | — |

二开对宿主的全部侵入面：**19 个 A 级新源文件 + 26 个 i18n locale 文件 + 27 个新测试文件 + 25 个宿主源文件修改点 + package.json/bun.lock**。desktop 主进程（`packages/desktop/src/process`）除 DB schema/migrations 外**零侵入**——所有"新 IPC 通道"实为 HTTP 桥（`httpBridge` → aioncore Rust 后端 / web-host 反代），无 Electron IPC handler 注册负担。

---

## 1. A 级：纯二开自有模块（可直接搬运）

迁移方式统一为"整文件搬运 + 校正 import 路径"；上游不存在同名文件，天然零冲突。

### 1.1 协议桥接层（common/adapter）

| 文件 | 来源 commit | 职责 | 导出符号 | 宿主依赖 | 被引用方 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| `packages/desktop/src/common/adapter/adHocTeamBridge.ts` | `8cbd1e833`（自 ipcBridge 抽离） | 封装两个 REST 端点：从普通会话建团 / 按会话查关联，含请求/响应 mapper | `adHocTeam`（`fromConversation`、`getByConversation`）、`toBackendAdHocTeamFromConversationParams`、`fromBackendAdHocTeamCreateResult`、`fromBackendAdHocTeamAssociationOptional` | `./httpBridge`（`httpGet/httpPost/withResponseMap`，宿主）、`./teamMapper`（`fromBackendTeam`，宿主） | `ipcBridge.ts:122,1959-1960`；`tests/unit/common-adapter/adHocTeamMapper.test.ts` | 单测 `adHocTeamMapper.test.ts` + HTTP 冒烟 `POST /api/teams/from-conversation`、`GET /api/teams/by-conversation` |
| `packages/desktop/src/common/adapter/teamPresetBridge.ts` | `ebca50b35`（自 ipcBridge 抽离） | Team Preset CRUD HTTP 适配层 + snake_case/别名/epoch→ISO mapper | `teamPreset`（`list/create/update/delete`）、`fromBackendTeamPreset*`、`toBackend*Input`、各 Params 类型 | `./httpBridge`（含 `httpPatch/httpDelete`）、`teamTypes` 的 preset 类型（C 级追加块） | `ipcBridge.ts:120-121`；`useTeamPresets.ts`（仅类型）；`ipcBridgeTeam.test.ts:128-146` | 单测验证 REST 路径 + 冒烟 `GET/POST/PATCH/DELETE /api/team-presets` |

两文件的设计意图即"独立于 ipcBridge 主文件以降低冲突面"（源码注释明示），是全 delta 中隔离做得最好的一层，也是 02 文档 adapter 方案的原型。

### 1.2 共享类型（common/types）

| 文件 | 来源 commit | 职责 | 导出符号 | 宿主依赖 | 被引用方 | 验证方式 |
| --- | --- | --- | --- | --- | --- | --- |
| `packages/desktop/src/common/types/team/adHocTeamTypes.ts` | `700fdf117` | 临时团队跨进程共享纯类型 | `IAdHocTeamFromConversationParams`、`IAdHocTeamByConversationParams`、`TAdHocTeamCreateResult`、`TAdHocTeamAssociation` | `./teamTypes`（`TTeam`、`WorkspaceMode`） | adHocTeamBridge、useAdHocTeamFromConversation、CollaborationLauncher、TeamStatusCard | `bunx tsc --noEmit` |

迁移注意：字段 snake_case（`team_id`/`origin_conversation_id`/`target_slot_id`）与 aioncore 响应一一对应，是后端硬契约；`target_assistant_name` 为前端组装补充（后端不返回）。

### 1.3 临时团队 UI（renderer/pages/conversation）

| 文件 | 来源 commit | 职责 | 宿主依赖（关键项） | 被引用方 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| `components/AdHocTeam/AdHocTeamSection.tsx` | `adf8dfaa4`（创建于 `700fdf117`） | 会话头部容器：未关联团队显示 CollaborationLauncher，已关联显示 TeamStatusCard；点击卡片 `navigate(/team/:id)` | Arco `Message`、`react-router-dom`、二开 hook 类型 | `ChatConversation.tsx:29`（aionrs 路径 `:220`、ACP 路径 `:383` 两处挂载） | `ChatConversation.adHocCollaboration.dom.test.tsx`、`AionrsConversation.adHocCollaboration.dom.test.tsx` |
| `components/AdHocTeam/AgentSelectorModal.tsx` | `700fdf117` | 选择拉入团队的 agent 的模态框 | 宿主 `@renderer/components/base/AionModal`、`pages/team/components/memberPicker/TeamAssistantPicker`、`assistantSelectUtils` | CollaborationLauncher | `AgentSelectorModal.dom.test.tsx` |
| `components/AdHocTeam/CollaborationLauncher.tsx` | `700fdf117` | 会话头部入口按钮 → AgentSelectorModal → 注入的 `create(assistantId)` | Arco、icon-park、宿主 hook `pages/team/hooks/useTeamAssistantOptions` | AdHocTeamSection | `CollaborationLauncher.dom.test.tsx` |
| `components/AdHocTeam/TeamStatusCard.tsx` | `700fdf117` | 团队状态卡片：队名/成员徽标/未读队友消息/最新预览/运行中 spin/活跃 turn 数 | Arco Badge/Button/Spin、宿主 `AgentStatusBadge`、teamTypes | AdHocTeamSection | `TeamStatusCard.dom.test.tsx` |
| `hooks/useAdHocTeamFromConversation.ts` | `700fdf117` | **核心状态 hook**：查询/创建会话关联团队，拉团队详情与 run-state，订阅 **15 个团队 WS 事件**保持源会话 UI 同步 | `@/common`（ipcBridge 的 `team.*` 二开 key + `team.get/getRunState` 等宿主 key）、teamTypes | `ChatConversation.tsx:28`（`:203`、`:284` 两处实例化） | `useAdHocTeamFromConversation.dom.test.ts` + `.status.dom.test.ts` |
| `hooks/useTeammateBackflow.ts` | `ea7757ce5` | `teammate_message` 流事件回流进成员会话消息列表：按 conversation_id 过滤、msg_id 去重、text 归一化后 merge | 宿主 `common/chat/chatLib`（`normalizeTextMessageContent`）、`Messages/hooks`（`useMergeLiveMessage`） | `useAcpMessage.ts:15,65,362`、`useAionrsMessage.ts:14,37,322` | `teammateBackflow.dom.test.tsx`、`AionrsConversation.teammateBackflow.dom.test.tsx` |
| `utils/conversationTeamOwnership.ts` | `adf8dfaa4` | 会话↔团队归属判定纯函数：读 `conversation.extra` 的 `team_id`（成员）与 `teamId`（promoted source） | 宿主 `common/config/storage`（`TChatConversation` 类型） | runtimeGate、ChatConversation、groupingHelpers（re-export）、useConversationActions | `conversationTeamOwnership.test.ts` |
| `utils/runtimeGate.ts` | `adf8dfaa4` | 运行时门控：promoted source 会话跳过独立 runtime ensure，生命周期交给团队会话 | 宿主 `./conversationCache`、`./ensureConversationRuntime` | `useAcpMessage.ts:19,561`、`AionrsSendBox.tsx:39,175` | `runtimeGate.test.ts` |

迁移注意（搬运时逐条核对）：

- `useAdHocTeamFromConversation` 订阅面极宽（`team.removed/renamed/agentStatusChanged/sessionChanged/teammateMessage/runAccepted/runStarted/runUpdated/runCompleted/runCancelled/runFailed/childTurnStarted/childTurnCompleted/childTurnCancelled/taskChanged`），上游 WS 事件名/载荷变动会直接破坏——这是 A 级模块对宿主最深的**接口级**依赖。
- `DEFAULT_USER_ID = 'system_default_user'` 在本 hook 与 `ChatConversation.tsx` 两处硬编码，搬运时应收敛为单点常量。
- `teammateMessage` 过滤依赖上游 `TeamAssistant.conversation_id` 字段。
- `runtimeGate.ts` 与 SendBox 内 `teamRuntime?.runtimeGate`（上游团队特性 `teamSendRuntime.ts` 的 `ConversationCommandQueueRuntimeGate`）**命名撞车但概念无关**，勿混淆。
- **疑似缺陷**：`TeamStatusCard` disbanded 分支 `onNavigate(association.origin_conversation_id)`，而父组件统一 `navigate(/team/${id})`，会把会话 id 填进团队路由（见 §7 技术债）。

### 1.4 专家团 UI（renderer/pages/team/TeamPresets）

| 文件 | 来源 commit | 职责 | 宿主依赖（关键项） | 被引用方 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| `TeamPresets/index.ts` | `e3f154559` | barrel 导出 | 仅内部 | `TeamCreateModal.tsx:21-28` | — |
| `TeamPresets/types.ts` | `e3f154559` | re-export `TeamPreset`/`TeamPresetMember`（真实定义在 C 级 teamTypes 追加块） | `teamTypes` | 内部组件 | `tsc` |
| `TeamPresets/hooks/useTeamPresets.ts` | `ebca50b35` | SWR 列表 + 乐观更新 CRUD（SWR key `team-presets/${userId}`） | `@/common`（ipcBridge）、宿主 `AuthContext.useAuth`、`common/utils/utils.uuid`、swr | barrel → TeamCreateModal | `useTeamPresets.dom.test.tsx` |
| `components/TeamPresetEditorModal/index.tsx`（372 行） | `e3f154559` | 新建/编辑专家团：名称/分类/描述/专长标签/示例任务/成员/Leader | 宿主 `AionModal`、`useTeamAssistantOptions`、`TeamAssistantPicker`、`TeamMemberDraftList`、`assistantSelectUtils` | barrel → TeamCreateModal（`:477-484`） | `TeamCreateModal.presets.dom.test.tsx` |
| `components/TeamPresetPicker/index.tsx`（155 行） | `e3f154559` | 左栏 preset 列表：选中/调用/新建/编辑/删除 | Arco、icon-park | TeamCreateModal（`:310-320`） | `TeamPresetPicker.dom.test.tsx` |
| `components/TeamPresetPreview/index.tsx` | `e3f154559` | 右栏详情预览 + "调用此专家团"（missing 时禁用） | Arco、内部 MemberList | TeamCreateModal（`:360-364`、`:419-423`） | 同上 |
| `components/TeamPresetMemberList/index.tsx` | `e3f154559` | 只读成员列表（Leader Crown、missing 标红） | Arco Tag | Preview | 同上 |
| `components/TeamPresetEmptyState/index.tsx` | `e3f154559` | 空态占位 | Arco Empty | **仅 barrel（死导出，见 §7）** | — |
| `pages/team/components/TeamCreateModal.module.css` | `e3f154559` | 修复 Arco Tabs 高度塌陷（`teamCreateTabs`） | **`:global()` 覆写 `.arco-tabs-*` 内部类——Arco 版本耦合脆弱点** | `TeamCreateModal.tsx:19,337` | DOM 测试目检 |

迁移注意：Leader 判定用 `assistant_id + order` 双键；EditorModal `wrapStyle zIndex 10001` 压 TeamCreateModal（10000）——层级耦合；`useTeamPresets(_userId)` 入参被忽略（实际取 `useAuth`）；v2.1.52 worktree 已把这组组件改为 `pages/team/components/TeamPreset*.tsx` 平铺 + 新增 `TeamPresetPanel.tsx`，**未来扩展包应以 worktree 的平铺布局为准**（少一层目录、与宿主组件同域）。

### 1.5 测试资产（27 个新文件，随对应模块同级搬运）

common-adapter：`adHocTeamMapper.test.ts`。
conversation：`AcpChat.adHocRunning`、`AgentSelectorModal`、`AionrsConversation.adHocCollaboration`、`AionrsConversation.teammateBackflow`、`ChatConversation.adHocCollaboration`、`CollaborationLauncher`、`ConversationSearchPopover`、`TeamStatusCard`、`conversationTeamOwnership.test.ts`、`groupingHelpers.test.ts`（新增版）、`runtimeGate.test.ts`、`teammateBackflow`、`useAdHocTeamFromConversation.dom` + `.status.dom`、`useConversationActionsDelete`、`useConversationListSync.adHocFilter`、`searchFilter.test.ts`。
team：`TeamCreateModal.presets`、`TeamPageOriginNavigation`、`TeamPresetPicker`、`TeamSiderSection`、`useTeamPresets`。
其他：`SiderItem.dom`、`useAcpConfigOptions.dom`、`useAionrsMessage.dom`、`process/services/database/migrations.test.ts`（新增，覆盖 v27）。
E2E 注意：`tests/e2e/specs/team-create-preset-leader.e2e.ts` 名字易误导，实为**上游资产**（assistant leader 选择，#3395/219780d05），不计入二开。

---

## 2. B 级：宿主插槽/入口（轻适配）

共同特征：hunk 为**追加式**（import、hook 调用、JSX 尾部插入、可选 prop），不删改上游行；适配方式 = 在新基线重新定位锚点插入。

| 宿主文件 | 插入位置（当前行号） | 插入内容 | 服务的二开模块 | 冲突风险 | 验证方式 |
| --- | --- | --- | --- | --- | --- |
| `pages/conversation/components/ChatConversation.tsx`（+31/−0） | aionrs 面板 `headerExtra`（`:220-227`）；主面板模型选择器行（`:383-392`）；hook 实例化 `:201-203`、`:270,284` | 两处 `<AdHocTeamSection>` 挂载；`adHocTeam.isTeamRunning` 透传 `<AcpChat>`（含 useMemo 依赖） | AdHocTeam 全部 | 中（上游活跃 JSX 区） | `ChatConversation.*.dom.test.tsx` |
| `platforms/acp/AcpChat.tsx`（+3/−0） | props/解构/`:99` | `isTeamRunning` prop 透传 | 同上 | 低 | `AcpChat.adHocRunning.dom.test.tsx` |
| `platforms/acp/AcpSendBox.tsx`（+3/−1） | `:696` | `disabled={isTeamRunning}`——团队运行中禁源会话输入 | 同上 | 低 | `AcpSendBox.dom.test.tsx` |
| `platforms/aionrs/AionrsChat.tsx`（+7/−0） | 组件顶部 `:60-65` | `useTeamPermission().warmupSession()` effect | 团队权限预热 | 低 | `AionrsConversation.*.dom.test.tsx` |
| `platforms/aionrs/AionrsSendBox.tsx`（+3/−3） | `:175`、`:214` | `ensureConversationRuntime`→`ensureStandaloneConversationRuntime`；`prepareRuntime` 无条件传入 | runtimeGate | 低 | `AionrsSendBox.dom.test.tsx` |
| `platforms/aionrs/useAionrsMessage.ts`（+5/−2） | `:37`、`:321-323` | 新增 `case 'teammate_message'` → `handleTeammateMessage`（上游本无此分支） | useTeammateBackflow | 低（纯新增 case） | `useAionrsMessage.dom.test.ts` |
| `pages/team/TeamPage.tsx`（+15/−3） | `headerLeading` `:446-460` | 临时团队页前置"返回来源会话"按钮（`data-testid='team-back-to-origin'`） | ad-hoc 导航 | 低-中（prop 值整体替换） | `TeamPageOriginNavigation.dom.test.tsx` |
| `components/layout/Sider/SiderItem.tsx`（+2/−1） | 类型 `:18`、菜单 `:113` | `SiderMenuItem.disabled?: boolean` 透传 | TeamSiderSection 删除中禁用 | 低 | `SiderItem.dom.test.tsx` |
| `GroupedHistory/ConversationRow.tsx`（+1/−1） | `:236` | `onDelete(id)` → `onDelete(id, extra)` | 删除确认分流 | 低 | `useConversationActionsDelete.dom.test.ts` |
| `GroupedHistory/types.ts`（+1/−1） | `:60` | `onDelete` 签名加 `extra?` | 同上 | 低 | `tsc` |
| `GroupedHistory/ConversationSearchPopover.tsx`（+3/−1） | `:196` | 搜索结果 `filter(!isTeamMemberConversation)`——成员会话不进搜索 | conversationTeamOwnership | 低（客户端过滤影响分页语义，上游改分页需留意） | `ConversationSearchPopover.dom.test.tsx` |
| `GroupedHistory/hooks/useConversationListSync.ts`（+3/−4） | `:145` | 过滤谓词改为只隐藏 `team_id` 成员会话，**promoted source 保留可见**（临时团队核心行为变更） | 同上 | 低 | `useConversationListSync.adHocFilter.dom.test.ts` |
| `GroupedHistory/utils/groupingHelpers.ts`（+10/−8） | `:92-106` | 删私有 `isTeamConversation`，re-export 共享归属模块并替换过滤谓词 | 同上 | 低 | `groupingHelpers.test.ts` |

---

## 3. C 级：共享类型/状态/Team 创建基础设施（中高适配）

共同特征：hunk **与宿主逻辑交织**（改写既有分支/重构 JSX），自动合并不可靠，需人工 review。

| 宿主文件 | 修改内容 | 冲突风险与原因 | 适配方式 | 验证方式 |
| --- | --- | --- | --- | --- |
| `pages/team/components/TeamCreateModal.tsx`（+243/−66，**全 delta 最深宿主改写**） | Arco `Tabs` 双模式（assistants/presets）包裹桌面双栏与移动单栏 body；右栏随 mode 切换 `TeamPresetPreview`；`handleInvokePreset`→`mapPresetToDrafts` 回填并切回 assistants 模式；`TeamPresetEditorModal` 作兄弟节点挂 AionModal 外；zIndex 10001/10000 约定 | **高**——两个 body JSX 结构性包裹 + return 改 fragment 双 Modal；上游任何 JSX 改动几乎必撞。文件头 `[E2E SYNC]` 注释要求 DOM 变更同步 e2e selector | 以"slot patch"重放（见 02 文档）；v2.1.52 worktree 已按新基线重做此文件，直接对照其版本 | `teamCreateModal.dom.test.tsx`、`TeamCreateModal.presets.dom.test.tsx` |
| `components/layout/Sider/TeamSiderSection.tsx`（约 +75/−23） | `deletingTeamId` 删除流禁用；`isAdHoc=!!origin_conversation_id` 换 Comment 图标 + tooltip；删除成功后跳回来源会话 | **中**——展开态把内联 icon 三元重构成 `siderIcon/siderNode` 常量并重排 JSX、删除 onOk 整体重写 | 人工合并；优先保留上游结构、重插 ad-hoc 分支 | `TeamSiderSection.dom.test.tsx`、`layout/TeamSiderSection.dom.test.tsx` |
| `platforms/acp/useAcpMessage.ts`（+16/−14） | 内联 `teammate_message` 处理**删除**，替换为 `useTeammateBackflow` 的 `handleTeammateMessage`（补 msg_id 去重）；`ensureConversationRuntime`→`ensureStandaloneConversationRuntime`（`:561,570,601`） | **中**——case 是改写非追加（上游已有无去重内联版）；符号级替换调用点 | 人工合并：上游若更新该 switch，需重新执行"抽 hook + 去重"改写 | `useAcpMessage.dom.test.ts`、`teammateBackflow.dom.test.tsx` |
| `pages/settings/AgentSettings/InlineAgentEditor.tsx`（+9/−1） | `behavior_policy` 解析从"整体赋值"改为逐字段构建（`supports_side_question/supports_team/team_capable_override`） | **中**——改写既有 if 块结构 | 人工合并 | `tsc` + 设置页手测 |
| `hooks/agent/useAcpConfigOptions.ts`（+8/−2） | loader 先读缓存，`extra.teamId` 会话直接返回 `[]`（团队会话不展示单人 config options） | **中**——函数体整体改写（自包含、合并容易） | 人工合并 | `useAcpConfigOptions.dom.test.ts` |
| `GroupedHistory/hooks/useConversationActions.ts`（+9/−5） | `handleDeleteClick` 加 `extra` 参；promoted source 删除换强警告文案组（`deleteTeamSource*`，warning→danger） | 低-中 | 条件分支重插 | `useConversationActionsDelete.dom.test.ts` |
| `common/types/team/teamTypes.ts`（+33/−0） | `TTeam.origin_conversation_id?`；文件尾追加 `TeamPresetMember`/`TeamPreset` 区块（`:255-285`） | 极低（尾部追加） | 尾部块整体搬运 | `tsc` |
| `common/types/platform/acpTypes.ts`（+5/−1） | `CustomAgentAdvancedOverrides.behavior_policy` 加 `supports_team`、`team_capable_override` | 低 | 字段重插 | `tsc` |
| `renderer/utils/model/agentTypes.ts`（+2/−0） | `BehaviorPolicy` 镜像加同两字段 | 极低 | 字段重插 | `tsc` |
| `common/adapter/teamMapper.ts`（+1/−0） | `fromBackendTeam` 透传 `origin_conversation_id`（`:112`） | 极低 | 单行重插 | `teamMapper.test.ts` |
| `services/i18n/index.ts`（+6/−2） | `getLocalStorageLanguageHint` 加 localStorage 守卫 + try/catch（加固，与功能无直接耦合） | 低 | 可放弃或单独成补丁 | `tsc` |
| `services/i18n/i18n-keys.d.ts`（+49/−0） | `I18nKey` union 三处按字母序插入 49 个 key | 低（机械化文本冲突，git 通常自动合并） | `bun run i18n:types` 重新生成 | `node scripts/check-i18n.js` |

---

## 4. D 级：IPC/API/数据库迁移/构建（关键适配）

### 4.1 协议出口挂载点

| 项 | 位置 | 内容 | 触发器 | 验证 |
| --- | --- | --- | --- | --- |
| ipcBridge 挂载 | `common/adapter/ipcBridge.ts:120-122`（import + re-export `teamPreset`/`adHocTeam`）、`:1959-1960`（`team.fromConversation`/`team.getByConversation`）、`:863-867,884-888`（`agent.createCustomAgent/updateCustomAgent` 的 `behavior_policy` 内联类型扩展） | 3 行 import + 2 行对象成员 + 类型小扩展——fork 刻意把实现隔离在 bridge 文件 | 上游重写 ipcBridge 的 team 命名空间或 behavior_policy 类型 | `ipcBridgeTeam.test.ts`、`adHocTeamMapper.test.ts`、`teamMapper.test.ts` |

约束（沿用既有决策）：`teamMapper.ts` + `ipcBridge.ts` 是**唯一协议出口**，禁止新增页面级 API 调用和裸 WebSocket 订阅。

### 4.2 后端 REST 契约（aioncore fork 侧实现，AionUi 仅消费）

| 端点 | 消费方 | 契约要点 |
| --- | --- | --- |
| `POST /api/teams/from-conversation` | `adHocTeam.fromConversation` | body `{conversation_id,user_id,target_assistant_id}`；响应 `{success,data}` envelope |
| `GET /api/teams/by-conversation?conversation_id=&user_id=` | `adHocTeam.getByConversation` | 非法 status 被前端静默归为 `'active'` |
| `GET/POST/PATCH/DELETE /api/team-presets[/{id}]` | `teamPreset.*` | 字段别名兜底（`assistant_backend/backend`、`assistant_name/name`）；`created_at/updated_at` 为 epoch 数字（bridge 转 ISO）；`leader/members/expertise_tags/example_prompts` 为 JSON 列 |

触发器：AionCore 升级偏离路由字符串、envelope 形状或字段别名即静默产生空字段。验证：对打包二进制做 HTTP 冒烟（区分"路由字符串存在"与"由目标 source commit 构建且冒烟通过"），并核对 bundle manifest `version/sourceType/source` + SHA256 与兼容矩阵同行。

### 4.3 WS 事件契约

`useAdHocTeamFromConversation` 订阅 15 个团队事件（清单见 §1.3）；`useTeammateBackflow` 消费 `teammate_message`。全部经宿主 ipcBridge emitter，上游事件名/载荷变动即破坏。验证：`*.status.dom.test.ts`、`teammateBackflow.dom.test.tsx`。

### 4.4 数据库迁移（双仓两层，均须遵守"M+1 起、不预留空号、已发布不改号"规则）

| 层 | 位置 | 二开内容 | 风险 |
| --- | --- | --- | --- |
| desktop 本地 DB（better-sqlite3） | `process/services/database/migrations.ts:1224-1248` `migration_v27`（`ALL_MIGRATIONS` 注册于 `:1258`）（teams 加 `origin_conversation_id` + 幂等 PRAGMA 检查；`down` 用 backup/rename **硬编码列清单**）；`schema.ts:96,103,165`（DDL 列 + 索引 + `CURRENT_DB_VERSION 26→27`） | **全 delta 最高危点**：上游（v26 之后）一旦发布自己的 v27，`ALL_MIGRATIONS` 撞号 + `CURRENT_DB_VERSION` 单行必撞；已升级用户库需补迁移路径；`down` 硬编码列清单合并上游新列后必须同步，否则 rollback 丢列 | `tests/unit/process/services/database/migrations.test.ts` |
| AionCore 后端 DB | `crates/aionui-db/migrations/`（AionCore 仓）：现行 `038_ad_hoc_team_origin_conversation.sql`、`039_team_presets.sql`、`040_backfill_formal_team_leader_team_id.sql`（v0.1.62 上游 max=037） | 编号随上游窗口重排是已知反复撞号点（历史：028/029→030/031/032→034/035/036/037→038/039/040；已删除 `037_enable_grok_build_team`，上游 `033_team_capability_criteria` 为超集）；存量库修复用 `scripts/migration/repair-legacy-versions.sh`（checksum+version 同事务） | AionCore 侧迁移测试 + 成对冒烟 |

归属键存储事实：成员会话 `extra.team_id`、promoted source `extra.teamId`、teams 表 `origin_conversation_id` 均由 **aioncore 后端写入**（二进制内可见 `json_set(extra,'$.team_id',...)` 回填 SQL 与 `COALESCE(json_extract(extra,'$.teamId'), json_extract(extra,'$.team_id'))` 查询）；前端**只读不写**，无任何写入点。键名大小写不对称（蛇形 vs 驼峰）是硬字符串契约，上游改 extra schema 即破坏。

### 4.5 构建配套

| 项 | 内容 | 触发器 | 验证 |
| --- | --- | --- | --- |
| `package.json` / `bun.lock`（`cc9f05549`、`b19a68072`） | `builder-util-runtime` 9.5.1→9.7.0 + `resolutions` 钉版 | 上游升级 electron-builder 链 | `bun install` + 打包冒烟 |
| bundle manifest | `resources/bundled-aioncore/`：aioncoreVersion pin × manifest(version/sourceType/source) × binary SHA256 × Core tag/SHA 四者同行入兼容矩阵 | 每次成对升级 | P6 兼容矩阵核对 + 冒烟 |
| `patches/2026-07-30-feat-team-add-reusable-expert-team-presets.patch` | 专家团单提交固化补丁（31 文件 +2681/−87，从 `e3f154559` format-patch 导出，`git apply --check --reverse` 验证过） | 仅作历史重放输入；现行策略已放弃补丁重放改基线重做 | — |

---

## 5. i18n 资产

49 个新 key × 13 语言 × 2 模块（`team.json`、`conversation.json`），locale 文件共 26 个、净增 704 行（机械化翻译）。
key 分布：`conversation.collaboration.*` ×13、`conversation.history.deleteTeamSource*` ×3、`team.presets.*` ×31、`team.sider.adHocTooltip`、`team.sider.deleting`。
语言：de-DE/en-US/es-ES/fa-IR/fr-FR/ja-JP/ko-KR/pt-BR/ru-RU/tr-TR/uk-UA/zh-CN/zh-TW。
验证：`bun run i18n:types && node scripts/check-i18n.js`。迁移方式：按 key 前缀整块导出/导入，勿逐行合并。

## 6. 上游变更触发器矩阵（上游动什么 → 二开哪里疼）

| 上游变更 | 受影响的二开项 | 级别 |
| --- | --- | --- |
| 发布本地 DB migration v27+ | `migrations.ts`/`schema.ts` 撞号，须重编号 + 存量库路径 | **D** |
| AionCore 上游 max migration 前移 | 038/039/040 整体平移 + checksum 修复 | **D** |
| 改 team WS 事件名/载荷、`TeamAssistant.conversation_id` | `useAdHocTeamFromConversation`（15 订阅）、`useTeammateBackflow` | A（接口级） |
| 改 `/api/teams/*`、`/api/team-presets` 契约 | 两个 bridge 的 mapper（别名/epoch 兜底失效） | A（接口级） |
| 改 conversations `extra` schema 或归属键名 | `conversationTeamOwnership` 全线（历史过滤/删除确认/门控） | A（契约级） |
| 重构 `TeamCreateModal` JSX | C 级最深改写，必人工合并 | **C** |
| 改 `useAcpMessage` 的 `teammate_message` 分支或 ensure 调用点 | C 级改写需重放 | **C** |
| 改 `ChatConversation` headerExtra/模型选择器行、`TeamSiderSection` 展开态 JSX/删除流、`TeamPage` headerLeading | B 级锚点重定位 | B |
| 重构团队页 memberPicker/AionModal/useTeamAssistantOptions | AgentSelectorModal、TeamPresetEditorModal 连带 | A（依赖级） |
| Arco 升级改 Tabs DOM 结构 | `TeamCreateModal.module.css` 的 `:global()` 覆写失效 | A（样式级） |
| 升级 electron-builder 链 | builder-util-runtime 钉版复核 | D |

## 7. 已知缺陷与技术债（搬运前宜顺手处理）

1. `TeamStatusCard` disbanded 分支疑似把 `origin_conversation_id` 填进 `/team/:id` 路由（父组件统一拼 `/team/${id}`）。
2. `DEFAULT_USER_ID='system_default_user'` 双处硬编码（hook 与 ChatConversation）。
3. `TeamPresetPicker` 搜索框 UI 存在但未接过滤逻辑（无 onChange/onSearch）。
4. `TeamPresetEmptyState` 仅 barrel 引用，死导出。
5. `useTeamPresets(_userId)` 忽略入参；编辑器提交 `user_id:''` 依赖后端回填。
6. EditorModal zIndex 10001 与 TeamCreateModal 10000 的层级耦合；`TeamCreateModal.module.css` 的 Arco `:global()` 覆写。
7. `migration_v27.down` 硬编码 teams 列清单，上游加列后必须同步。
8. v2.1.52 worktree 的临时团队会话 UI（AdHocTeam 组件、回流/门控工具、SendBox 接线）**仍未提交**，重做未收口。

## 附录：历史文档关系

- `2026-07-22-...独立维护与上游同步计划.md`：工作流原则有效（双仓成对、编号规则、成对回滚）；"逐版本 cherry-pick 重建"策略已被基线重做取代。
- `2026-07-30-...模块化改造分析报告.md`：战略依据仍有效；其 A–E 分层（跨双仓按层切分）与任务清单版 A–E（提交切片）口径不同，引用须注明。
- `2026-07-30-...补丁包维护与窗口化升级实施任务清单.md`：§12 落地记录/重编号分析权威；§13 窗口 C 未执行即被取代；其 §2 A 包含 desktop DB 条目属早期描述，与现行"迁移全在 AionCore"并存时需区分两层（desktop 本地 v27 仍真实存在于当前 delta）。
- `2026-08-09-最新基线重做...方案.md` + `P6 兼容矩阵与回滚手册.md`：当前主方案与权威矩阵，本文档引用而非复制。
