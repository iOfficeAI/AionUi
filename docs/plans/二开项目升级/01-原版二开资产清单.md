# 01 - 原版二开资产清单（PIN 树可核验）

> 建立：2026-08-09 · 任务 019fe712-0281-73c0-bf78-aac596023062 · 2026-08-10 P8-1 物理分层（原 `01-二开代码资产与依赖分级.md`，见 `legacy/`）
> 本文**只写原版事实**：全部条目以固定证据树核验为准，不含 v2.1.52 重做线状态；适配状态与移植判定见 [`02-v2.1.52适配矩阵.md`](./02-v2.1.52适配矩阵.md)，扩展包与 adapter 决策（含 DR-1）见 [`03-扩展包与adapter决策.md`](./03-扩展包与adapter决策.md)。
>
> 数据口径（唯一原版证据源）：
> - **AionUi 原版**：`integrate/ad-hoc-team-latest` 固定 SHA `adf8dfaa432625cd1688459b2990156140be1fd7`（PIN-UI）；文档口径基线 `b397e15fd` 与 PIN-UI 仅隔 2 个 docs 提交、产品代码完全一致。基线 AionUi v2.1.40 × AionCore v0.1.51，二开增量 = `git diff upstream/main...HEAD`（64 新文件 + 45 个修改源文件 + 26 locale + 27 新测试）。**复算注记**（来源审计 2026-08-10 §9-1）：本地浅克隆无 `upstream/main...HEAD` merge-base，无法原法复算；替代法（汇总 20 个二开/文档提交自身足迹）精确命中 27 测试与 26 locale，64/45 待全量克隆重算。
> - **AionCore 原版**：同名分支固定 SHA `eb0c884ecbed47f96d5a80b0ea603933fd4cf668`（PIN-CORE，即该分支当前 HEAD）。
> - **专家团原版**：`e3f154559`（PIN-UI 祖先，独立核验：31 文件 +2681/−87，纯 AionUi 前端）；固化补丁 `patches/2026-07-30-feat-team-add-reusable-expert-team-presets.patch`（自 `e3f154559` 导出）。
> - **审计证据**：`/tmp/adhoc-team-ui-audit-v2.1.52.md`（任务 019fe703，临时团队 UI 高保真审计）——本文吸收其结论，并对两处与源码不符之处做了勘误（见 §1.3）；来源审计 `/tmp/audit-2dev-docs-provenance-20260810.md`（任务 019fe97f）逐项核验本文与 PIN 树一致。
> - **溯源**：旧原型分支 `feat/ad-hoc-team-context` @ `8b2953a33`（已被 `700fdf117` 取代，仅作溯源）。
>
> 配套机读清单：[`01-原版二开资产清单.manifest.json`](./01-原版二开资产清单.manifest.json)（schema `aionui-2dev-asset-inventory/2`，`originalSource`/`adapterTarget` 双层）。

## 1. 范围、分级与证据勘误

### 1.1 分级定义

| 级别 | 定义 | 迁移成本 | 数量（源文件） |
| --- | --- | --- | --- |
| **A** | 纯二开自有模块，上游无同名文件 | 直接搬运 | 19（临时团队 10 + 专家团 9）+ 27 测试 |
| **B** | 宿主插槽/入口，追加式 hunk | 锚点重定位 | 12 |
| **C** | 共享类型/状态/Team 创建基础设施，与宿主逻辑交织 | 人工合并 | 12 |
| **D** | IPC/API/DB 迁移/构建配套 | 走兼容矩阵与成对升级 | 4 组 |

### 1.2 原版提交溯源链

| 功能 | 原版提交链（PIN-UI 祖先） |
| --- | --- |
| 临时团队 | `700fdf117`（原型，溯源 `8b2953a33`）→ `adf8dfaa4`（runtime ownership 收口）→ `8cbd1e833`（抽 adHocTeamBridge）→ `ea7757ce5`（回流收口）；测试 `bee2093d9`/`35464bd9e`/`7f38664ae`/`a7efe3fb5`/`5c459df38`/`14d11cc6e` |
| 专家团 | **`e3f154559`（落地，含 TeamPresets 8 文件 + TeamCreateModal 改造 + i18n + 测试）** → `ebca50b35`（抽 teamPresetBridge） |
| ACP 团队能力覆盖 | `63ffc91e9` |
| 构建对齐 | `cc9f05549`、`b19a68072` |
| 本地 DB 迁移 | `700fdf117`（migration_v27） |

v2.1.52 重做线的逐提交对应见 `02-v2.1.52适配矩阵.md` §1。

### 1.3 对审计证据的勘误（以源码为准）

1. **事件订阅数为 15 而非 17**：`useAdHocTeamFromConversation.ts:185-211` 实际订阅 removed/renamed/agentStatusChanged/sessionChanged/teammateMessage/runAccepted/runStarted/runUpdated/runCompleted/runCancelled/runFailed/childTurnStarted/childTurnCompleted/childTurnCancelled/taskChanged（grep `.on(` = 15）。审计文件 §1.2/§2.5/§5 的"17 个"系笔误。
2. **`createError.codes.TEAM_*`（3 个）与 `agentError.codes.TEAM_*`（4 组 title/body）为上游资产**：由上游 PR #3395（`72129a5e8`）引入，登记在上游 `i18n-keys.d.ts`，不计入二开 49 个新增 key。审计 §2.8 将其列入规格，移植时无需搬运（新基线自带）。

---

## 2. A 级：纯二开自有模块（19 + 27 测试）

### 2.1 临时团队——协议与类型（2）

| 文件（行数） | 来源 | 职责 / 导出 | 宿主依赖 | 消费方 | 验证 |
| --- | --- | --- | --- | --- | --- |
| `common/adapter/adHocTeamBridge.ts`（74） | `8cbd1e833` | 两个 REST 端点封装 + mapper：`adHocTeam.fromConversation`（POST `/api/teams/from-conversation`）、`adHocTeam.getByConversation`（GET `/api/teams/by-conversation`）；`toBackendAdHocTeamFromConversationParams`、`fromBackendAdHocTeamCreateResult`、`fromBackendAdHocTeamAssociationOptional` | `./httpBridge`（httpGet/httpPost/withResponseMap）、`./teamMapper`（fromBackendTeam） | `ipcBridge.ts:121-122,1959-1960` | `tests/unit/common-adapter/adHocTeamMapper.test.ts` + HTTP 冒烟 |
| `common/types/team/adHocTeamTypes.ts`（41） | `700fdf117` | 纯类型：`IAdHocTeamFromConversationParams`、`IAdHocTeamByConversationParams`、`TAdHocTeamCreateResult`、`TAdHocTeamAssociation`（snake_case 与 aioncore 响应硬契约） | `./teamTypes`（TTeam、WorkspaceMode） | bridge、hook、Launcher、StatusCard | `bunx tsc --noEmit` |

### 2.2 临时团队——UI 组件（4，审计 §2.1-2.4 规格本体）

| 文件（行数） | 职责与交互规格要点 | 宿主依赖 | 消费方 | 验证（用例数） |
| --- | --- | --- | --- | --- |
| `components/AdHocTeam/CollaborationLauncher.tsx`（90） | 会话头部「发起团队协作」入口：触发钮（Tooltip+Button text/mini，`data-testid='collaboration-launcher-trigger'`，Peoples 图标）→ AgentSelectorModal → `create(assistant.id)`；失败 `Message.error(createFailed)` **弹窗保持打开**；提交中禁关窗。Props 中 `conversationId/userId` 被 `_` 忽略（冗余签名，§9-4） | Arco、宿主 `pages/team/hooks/useTeamAssistantOptions` | AdHocTeamSection | `CollaborationLauncher.dom.test.tsx`（11 例） |
| `components/AdHocTeam/AgentSelectorModal.tsx`（104） | 纯受控 agent 选择弹窗：`AionModal variant='standard' size='medium'` + TeamAssistantPicker（`density='modal'`、`testIdPrefix='agent-selector'`）；confirmLoading 拦截取消；isLoading 居中 Spin；已选中显示 joinedHint 条 | 宿主 `AionModal`、`memberPicker/TeamAssistantPicker`、`assistantSelectUtils` | CollaborationLauncher | `AgentSelectorModal.dom.test.tsx`（8 例） |
| `components/AdHocTeam/AdHocTeamSection.tsx`（75） | 组装层：`isMobile\|\|isReadOnly`→null；非团队会话→Launcher（成功 toast joinedSuccess）；`association?.team_id`→TeamStatusCard；`onNavigate=(teamId)=>navigate('/team/'+teamId)` | Arco Message、react-router-dom、二开 hook | **`ChatConversation.tsx` 两处挂载（B 级 §3.1）** | `ChatConversation.adHocCollaboration.dom.test.tsx`（9 例）、`AionrsConversation.adHocCollaboration.dom.test.tsx`（7 例） |
| `components/AdHocTeam/TeamStatusCard.tsx`（138） | 已关联团队紧凑状态卡：四行（标题行=statusTitle+AgentStatusBadge+Disbanded+running Spin+成员数 Badge+未读 Badge／团队名 truncate／最新消息预览／runDetail N active turns）；active→`onNavigate(teamId)`，**disbanded→`onNavigate(origin_conversation_id)`**（回航意图，但父级拼 `/team/${id}` 致断链，§9-1） | Arco Badge/Button/Spin、宿主 `AgentStatusBadge` | AdHocTeamSection | `TeamStatusCard.dom.test.tsx`（18 例¹） |

### 2.3 临时团队——hooks 与纯逻辑（4）

| 文件（行数） | 职责 | 宿主依赖 | 消费方 | 验证 |
| --- | --- | --- | --- | --- |
| `hooks/useAdHocTeamFromConversation.ts`（284） | **核心状态 hook**：初始关联查询（空 id 跳过 + cancelled 竞态防护，userId 缺省 `system_default_user`）；team 详情 + `team.getRunState`（activeRun/slotWorkBySlot）；**15 个 `ipcBridge.team.*` WS 事件订阅**（`:185-211`，removed→重取关联＝"删除后回源"机制；renamed/agentStatusChanged/sessionChanged/taskChanged→重取 team；teammateMessage→最新消息+未读+1；run 六事件统一 handleRunEvent，accepted 回拉对账、终态清 activeRun；childTurn 增删）；`create`→fromConversation 就地更新 association、失败归一化 rethrow。已知冗余：`clearUnreadTeammateMessages` 导出无调用方（§9-3） | `@/common` ipcBridge（二开 key `team.fromConversation/getByConversation` + 宿主 key `team.get/getRunState`/15 事件 emitter）、teamTypes | `ChatConversation.tsx` 两处实例化（旧版 `:203`、`:284`） | `useAdHocTeamFromConversation.dom.test.ts`（8 例）+ `.status.dom.test.ts`（17 例¹） |
| `hooks/useTeammateBackflow.ts`（59） | `teammate_message` 回流：conversation_id 过滤 → msg_id Set 去重（无 id 不丢）→ `normalizeTextMessageContent` → `useMergeLiveMessage` 合并；导出 `acceptTeammateMessageId`、`normalizeTeammateMessage`、`useTeammateBackflow` | 宿主 `common/chat/chatLib`、`Messages/hooks` | `useAcpMessage.ts:65,362`、`useAionrsMessage.ts:37,322` | `teammateBackflow.dom.test.tsx`、`AionrsConversation.teammateBackflow.dom.test.tsx` |
| `utils/conversationTeamOwnership.ts`（29） | 归属判定纯函数族：`extra.team_id`（成员会话）/`extra.teamId`（promoted 源会话）；`getTeamMemberId/getPromotedSourceTeamId/isTeamMemberConversation/isPromotedTeamSourceConversation/isTeamRelatedConversation` | 宿主 `common/config/storage`（TChatConversation 类型） | runtimeGate、ChatConversation、groupingHelpers（re-export）、useConversationActions | `conversationTeamOwnership.test.ts` |
| `utils/runtimeGate.ts`（22） | `isTeamManagedRuntime`/`ensureStandaloneConversationRuntime`：promoted 源会话跳过独立 runtime ensure（生命周期交给团队会话） | 宿主 `./conversationCache`、`./ensureConversationRuntime` | `useAcpMessage.ts:561`、`AionrsSendBox.tsx:175` | `runtimeGate.test.ts` |

> ¹ 用例数统计口径注记（来源审计 2026-08-10 §9-3）：文档口径 18/17 例，`grep -c 'it('` 实测 21/19——或为 `it.each`/嵌套 describe 的统计口径差异，移植验收以测试文件实际通过数为准。

### 2.4 专家团——协议与 UI（9，覆盖 `e3f154559` 全量）

| 文件（行数） | 职责 | 宿主依赖 | 消费方 | 验证 |
| --- | --- | --- | --- | --- |
| `common/adapter/teamPresetBridge.ts`（115） | Preset CRUD HTTP 适配 + mapper（snake_case/别名兜底 `assistant_backend\|backend`、`assistant_name\|name`、epoch→ISO）：`teamPreset.list/create/update/delete` ↔ `GET/POST/PATCH/DELETE /api/team-presets[/{id}]` | `./httpBridge`、teamTypes preset 类型块（C 级） | `ipcBridge.ts:120-121`；useTeamPresets（类型） | `ipcBridgeTeam.test.ts:128-146`（REST 路径断言） |
| `TeamPresets/index.ts`（14） | barrel 导出 | 仅内部 | `TeamCreateModal.tsx:21-28` | — |
| `TeamPresets/types.ts`（7） | re-export `TeamPreset`/`TeamPresetMember` | teamTypes（C 级块 `:255-285`） | 内部组件 | `tsc` |
| `TeamPresets/hooks/useTeamPresets.ts`（160） | SWR（key `team-presets/${userId}`）列表 + 乐观更新 CRUD；`_userId` 入参被忽略（取 `useAuth`，§9-5） | `@/common` ipcBridge、宿主 `AuthContext`、`common/utils/utils.uuid`、swr | barrel → TeamCreateModal | `useTeamPresets.dom.test.tsx` |
| `TeamPresets/components/TeamPresetEditorModal/index.tsx`（372） | 新建/编辑：名称/分类/描述/专长标签/示例任务/成员/Leader（`assistant_id+order` 双键判定）；zIndex 10001 压主模态（§9-6） | 宿主 `AionModal`、`useTeamAssistantOptions`、`TeamAssistantPicker`、`TeamMemberDraftList`、`assistantSelectUtils` | TeamCreateModal `:477-484` | `TeamCreateModal.presets.dom.test.tsx` |
| `TeamPresets/components/TeamPresetPicker/index.tsx`（155） | 左栏列表：选中/调用/新建/编辑/删除；**搜索框未接过滤逻辑（§9-4）** | Arco Dropdown/Menu | TeamCreateModal `:310-320` | `TeamPresetPicker.dom.test.tsx` |
| `TeamPresets/components/TeamPresetPreview/index.tsx`（111） | 右栏详情预览 + 调用钮（missing 禁用） | Arco、内部 MemberList | TeamCreateModal `:360-364,:419-423` | 同上 |
| `TeamPresets/components/TeamPresetMemberList/index.tsx`（63） | 只读成员列表（Leader Crown / missing 标红） | Arco Tag | Preview | 同上 |
| `TeamPresets/components/TeamPresetEmptyState/index.tsx`（39） | 空态占位（**仅 barrel 引用，死导出 §9-4**） | Arco Empty | — | — |
| `pages/team/components/TeamCreateModal.module.css`（71） | 修 Arco Tabs 高度塌陷（`teamCreateTabs`）；**`:global()` 覆写 `.arco-tabs-*`（Arco 版本耦合 §9-6）** | — | `TeamCreateModal.tsx:19,337` | DOM 测试 |

数据模型（`e3f154559` 定义于 teamTypes 尾块）：`TeamPresetMember{assistant_backend, assistant_id?, model?, assistant_name, role, order}`；`TeamPreset{id, user_id, name, icon?, category?, description, expertise_tags[], example_prompts[], leader, members[], version, created_at, updated_at}`。持久化在 **AionCore 仓**（表 `team_presets`，原版迁移编号 **035** @ PIN-CORE，见 §5.3；desktop 本地 DB 无 preset 表）。

---

## 3. B 级：宿主插槽/入口（12 处，追加式）

> 本表只登记**旧版（PIN-UI）插入点**；对应的 v2.1.52 插入点/移植状态见 `02-v2.1.52适配矩阵.md` §3。

### 3.1 会话区（吸收审计 §1.2/§5）

| 宿主文件 | 旧版插入（行号） | 内容 | 风险 | 验证 |
| --- | --- | --- | --- | --- |
| `ChatConversation.tsx` | aionrs 分支 `:209-228`；ACP 分支 `:374-393`；hook `:203/:284`；`:309` isTeamRunning 透传 | 两处 `<AdHocTeamSection>` 挂载 + hook 实例化 + isTeamRunning 透传 | 中 | adHocCollaboration 两套 dom 测试 |
| `platforms/acp/AcpChat.tsx`（+3/−0） | props/解构/`:99` | `isTeamRunning` prop 透传 | 低 | `AcpChat.adHocRunning.dom.test.tsx` |
| `platforms/acp/AcpSendBox.tsx`（+3/−1） | `:695-696` | 团队运行中禁源会话输入 `disabled={isTeamRunning}`（仅 ACP；规格内 aionrs 无此 prop，§9-2） | 低 | `AcpSendBox.dom.test.tsx` |
| `platforms/aionrs/AionrsChat.tsx`（+7/−0） | `:60-65` | `useTeamPermission().warmupSession()` effect | 低 | AionrsConversation dom 测试 |
| `platforms/aionrs/AionrsSendBox.tsx`（+3/−3） | `:175`、`:214` | `ensureStandaloneConversationRuntime` 替换 + prepareRuntime 无条件传入 | 低 | `AionrsSendBox.dom.test.tsx` |
| `platforms/aionrs/useAionrsMessage.ts`（+5/−2） | `:321-323` 新增 case | aionrs 回流接入（`teammate_message` case） | 低 | `useAionrsMessage.dom.test.ts` |

### 3.2 团队页与侧栏、历史区

| 宿主文件 | 旧版插入（行号） | 内容 | 风险 | 验证 |
| --- | --- | --- | --- | --- |
| `pages/team/TeamPage.tsx`（+15/−3） | `headerLeading:447-456` | 临时团队页"返回来源会话"按钮（`data-testid='team-back-to-origin'`） | 低-中 | `TeamPageOriginNavigation.dom.test.tsx` |
| `Sider/SiderItem.tsx`（+2/−1） | 类型 `:18`、菜单 `:113` | `SiderMenuItem.disabled?` 透传 | 低 | `SiderItem.dom.test.tsx` |
| `GroupedHistory/ConversationRow.tsx`（+1/−1） | `:236` | `onDelete(id)`→`onDelete(id, extra)` | 低 | `useConversationActionsDelete.dom.test.ts` |
| `GroupedHistory/types.ts`（+1/−1） | `:60` | onDelete 签名配套 | 低 | `tsc` |
| `GroupedHistory/ConversationSearchPopover.tsx`（+3/−1） | `:196` | 搜索结果滤除成员会话（客户端过滤，注意分页语义） | 低 | `ConversationSearchPopover.dom.test.tsx` |
| `GroupedHistory/hooks/useConversationListSync.ts`（+3/−4） | `:143-152` | 只隐藏 `team_id` 成员会话，promoted 源保留可见（responseStream 全量注册防刷新循环的注释手法须保留） | 低 | `useConversationListSync.adHocFilter.dom.test.ts` |
| `GroupedHistory/utils/groupingHelpers.ts`（+10/−8） | `:92-125` | re-export 归属模块 + 过滤谓词替换 | 低 | `groupingHelpers.test.ts` |

---

## 4. C 级：共享类型/状态/创建基础设施（12 处，交织改写）

> 本表只登记**原版改写内容**；v2.1.52 适配方式见 `02-v2.1.52适配矩阵.md` §4。

| 宿主文件（diff） | 修改内容 | 风险 | 验证 |
| --- | --- | --- | --- |
| `pages/team/components/TeamCreateModal.tsx`（+243/−66，**最深改写**） | Tabs 双模式（assistants/presets）包裹桌面/移动 body；右栏随 mode 切 Preview；`handleInvokePreset`→`mapPresetToDrafts` 回填并切回 assistants；EditorModal 兄弟节点外挂（return 改 fragment）；文件头 `[E2E SYNC]` 约束 | **高** | `teamCreateModal.dom.test.tsx`、`TeamCreateModal.presets.dom.test.tsx` |
| `Sider/TeamSiderSection.tsx`（≈+75/−23） | `deletingTeamId` 删除流；`isAdHoc=!!origin_conversation_id` 换图标+tooltip；删除成功回源会话（`:283-300`，`replace:true`） | 中 | `TeamSiderSection` 两套 dom 测试（删 ad-hoc 回源/普通回首页/失败不跳/data-is-adhoc） |
| `platforms/acp/useAcpMessage.ts`（+16/−14） | 内联 `teammate_message` 处理删除→`useTeammateBackflow`（补去重）；`ensureConversationRuntime`→`ensureStandaloneConversationRuntime`（`:561,570,601`） | 中（改写非追加） | `useAcpMessage.dom.test.ts` |
| `pages/settings/AgentSettings/InlineAgentEditor.tsx`（+9/−1） | behavior_policy 逐字段构建（supports_side_question/supports_team/team_capable_override） | 中 | `tsc` + 设置页手测 |
| `hooks/agent/useAcpConfigOptions.ts`（+8/−2） | `extra.teamId` 会话跳过 runtime config options（返回 `[]`） | 中 | `useAcpConfigOptions.dom.test.ts` |
| `GroupedHistory/hooks/useConversationActions.ts`（+9/−5） | promoted 源删除换强警告文案组（deleteTeamSource*，warning→danger） | 低-中 | `useConversationActionsDelete.dom.test.ts` |
| `common/types/team/teamTypes.ts`（+33/−0） | `TTeam.origin_conversation_id?` + 尾部 `TeamPresetMember/TeamPreset` 块（`:255-285`） | 极低（尾追加） | `tsc` |
| `common/types/platform/acpTypes.ts`（+5/−1） | behavior_policy 加 `supports_team`、`team_capable_override` | 低 | `tsc` |
| `renderer/utils/model/agentTypes.ts`（+2/−0） | BehaviorPolicy 镜像两字段 | 极低 | `tsc` |
| `common/adapter/teamMapper.ts`（+1/−0） | `fromBackendTeam` 透传 `origin_conversation_id`（`:112`） | 极低 | `teamMapper.test.ts` |
| `services/i18n/index.ts`（+6/−2） | localStorage 守卫 + try/catch（加固，与功能无耦合） | 低 | `tsc` |
| `services/i18n/i18n-keys.d.ts`（+49/−0） | I18nKey union 三处字母序插入（collaboration×13、history×3、presets×31、sider×2） | 低（机械冲突） | `bun run i18n:types && node scripts/check-i18n.js` |

补充（审计 §2.6 规格）：sendbox 运行时门的宿主侧为**上游资产** `teamSendRuntime.ts`（187 行，`FATAL_BLOCK_REASONS={runtime_failed, removing}`，gate 恒 `hydrated:true`）与 `useConversationCommandQueue.ts:345-365`（`getCommandQueueExecutionGate` runtimeGate 优先）——二开只在其插口上接线（`disabled={isTeamRunning}`、teamSendMessage 分流 + emit `chat.history.refresh`/`acp.workspace.refresh`、ThoughtDisplay/stop 覆盖），**不可把上游文件本体列入二开资产**。

---

## 5. D 级：IPC/API/DB/构建（4 组）

### 5.1 协议出口挂载（ipcBridge）

`ipcBridge.ts:120-122`（`teamPreset` re-export + 两 import）+ `:1959-1960`（`team.fromConversation/getByConversation` 挂入 team 对象）+ `:863-867,:884-888`（`agent.createCustomAgent/updateCustomAgent` 的 behavior_policy 内联类型扩展）。约束（沿用既有决策）：`teamMapper.ts`+`ipcBridge.ts` 为唯一协议出口，禁止页面级 API 调用与裸 WS 订阅。回流链路（审计 §2.6）：`message.stream` WS → `httpBridge.ts:438-453` → `conversation.responseStream`（acp `:833`/aionrs `:991` 复用同一流）→ 两平台消息 switch；旁路 `team.teammateMessage` wsEmitter（`ipcBridge:2047`）→ 状态卡预览/未读。

### 5.2 后端契约（aioncore fork 实现，AionUi 仅消费）

- REST：`POST /api/teams/from-conversation`、`GET /api/teams/by-conversation`、`/api/team-presets` CRUD 五端点；envelope `{success,data}`；preset 字段别名兜底 + epoch 时间；`leader/members/expertise_tags/example_prompts` JSON 列。
- WS：15 个团队事件（§2.3 清单）+ `teammate_message` 流事件。
- 归属键：**aioncore 后端写入** `conversations.extra.team_id`（成员）/`extra.teamId`（promoted 源，大小写不对称是硬契约）与 `teams.origin_conversation_id`；前端只读不写。
- 触发器：路由字符串/envelope/别名/事件名/载荷/extra 键名任一变动即破坏。验证：HTTP 冒烟（区分"路由存在"与"目标 commit 构建且冒烟通过"）+ bundle manifest `version/sourceType/source`+SHA256 与兼容矩阵同行。

### 5.3 数据库迁移（双层，均守"M+1 起、不预留空号、已发布不改号"）

| 层 | 位置 | 二开内容 | 风险 |
| --- | --- | --- | --- |
| desktop 本地 DB | `migrations.ts:1224-1248` `migration_v27`（teams 加 `origin_conversation_id`，幂等 PRAGMA；`down` backup/rename **硬编码列清单**；注册 `:1258`）；`schema.ts:96,103,165`（`CURRENT_DB_VERSION 26→27`） | **最高危**：上游发布 v27 即撞号 + 常量单行必撞；存量库需补路径；`down` 列清单须随上游新列同步 | `tests/unit/process/services/database/migrations.test.ts` |
| AionCore 后端 DB（**原版窗口** @ PIN-CORE `eb0c884e`） | `crates/aionui-db/migrations/`：`034_ad_hoc_team_origin_conversation.sql`、`035_team_presets.sql`、`036_backfill_formal_team_leader_team_id.sql` | 编号历史 028→030→034/035/036；**`037_enable_grok_build_team.sql` 存在**（二开 `7bdd1b5e` 引入，PIN-CORE 未删）；**无 `repair-legacy-versions.sh`**（scripts/migration/ 仅 check-immutability 系列） | AionCore 迁移测试 + 成对冒烟 |

> 来源注记（来源审计 2026-08-10 §7.1，已复核 PIN-CORE 树）：本表只写原版窗口事实。v2.1.52 重做线的迁移窗口（同名平排 038/039/040、037 删除理由、repair 脚本 `e0e03ab8`）见 `02-v2.1.52适配矩阵.md` §5——两窗口各自成立，不可混写。

### 5.4 构建配套

`package.json`/`bun.lock`：builder-util-runtime 9.5.1→9.7.0 + resolutions 钉版（`cc9f05549`/`b19a68072`）。bundle：`aioncoreVersion` pin × manifest × SHA256 × Core tag 四者同行入 P6 兼容矩阵。补丁：`patches/2026-07-30-*.patch`（`e3f154559` 单提交固化，format-patch，`git apply --check --reverse` 验证过；仅历史重放输入，现行策略为基线重做）。

---

## 6. i18n 资产（二开 49 key × 13 语言）

> 本表只登记原版 key 清单与规格缺口；v2.1.52 移植状态见 `02-v2.1.52适配矩阵.md` §6。

| 组 | key 数 | 模块 |
| --- | --- | --- |
| `conversation.collaboration.*` | 13（launchTooltip/selectAgentTitle/createdSuccess/joinedSuccess/joinedHint/fallbackAgentName/createFailed/statusTitle/statusAria/latestMessage/disbanded/running/runDetail） | conversation.json |
| `conversation.history.deleteTeamSource{Title,Confirm,Ok}` | 3 | conversation.json |
| `team.presets.*` | 31 | team.json |
| `team.sider.adHocTooltip`、`team.sider.deleting` | 2 | team.json |

规格内缺口（审计 §2.8/§6-5）：`collaboration.running/runDetail` 仅 en-US/zh-CN 有译文，其余 11 locale 靠 fallback（移植时原样或补齐由 leader 决策）；`createdSuccess` 已定义未引用（UI 用 joinedSuccess）。13 语言 ×2 模块共 26 个 locale 文件、+704 行机械化翻译；迁移按 key 前缀整块导出/导入。验证：`bun run i18n:types && node scripts/check-i18n.js`。**注意 `TEAM_*` 错误码 key 为上游资产（§1.3-2），不在搬运范围。**

## 7. 上游变更触发器矩阵

| 上游变更 | 受影响二开项 | 级别 |
| --- | --- | --- |
| 发布本地 DB migration v27+ | migrations/schema 撞号，重编号 + 存量路径 | D |
| AionCore 上游 max migration 前移 | 二开迁移平移 + checksum 修复（原版 034/035/036；新版窗口平排见 02 §5） | D |
| 改 team WS 事件名/载荷、`TeamAssistant.conversation_id` | useAdHocTeamFromConversation（15 订阅）、useTeammateBackflow | A（接口级） |
| 改 REST 契约（路由/envelope/别名/epoch） | 两 bridge mapper | A（接口级） |
| 改 conversations `extra` 键名/schema | conversationTeamOwnership 全线 | A（契约级） |
| 重构 TeamCreateModal / ChatConversation / AcpSendBox | C/B 级最深接点 | C/B |
| 改 useAcpMessage teammate 分支或 ensure 调用点 | C 级改写重放 | C |
| 重构 memberPicker/AionModal/useTeamAssistantOptions | AgentSelectorModal、TeamPresetEditorModal | A（依赖级） |
| Arco 升级改 Tabs DOM | TeamCreateModal.module.css `:global()` 失效 | A（样式级） |
| 升级 electron-builder 链 | builder-util-runtime 钉版 | D |

## 8. 已知缺陷与技术债（合并审计 §6，移植时按 leader 决策）

1. **disbanded 回航断链**：TeamStatusCard disbanded→`onNavigate(origin_conversation_id)`，但 AdHocTeamSection 固定拼 `/team/${id}` → 实际导航 `/team/<conversationId>`，与 TeamPage/侧栏 `/conversation/<id>` 约定不一致；旧测试只断言回调参数未覆盖最终路由（两路独立审计均确认）。
2. **ACP/aionrs 不对称**：aionrs SendBox 无 `isTeamRunning` 禁用（规格内如此，团队运行时仍可输入，仅靠 runtimeGate/teamRuntime 约束）。
3. `clearUnreadTeammateMessages` 导出无调用方，未读徽章只增不清。
4. Launcher 的 `conversationId/userId` props 冗余；TeamPresetPicker 搜索框未接逻辑；TeamPresetEmptyState 死导出。
5. i18n：`createdSuccess` 未引用；`collaboration.running/runDetail` 仅中英。
6. EditorModal zIndex 10001/10000 层级耦合；`TeamCreateModal.module.css` Arco `:global()` 覆写脆弱。
7. `DEFAULT_USER_ID='system_default_user'` 双处硬编码（hook 与 ChatConversation）。
8. `migration_v27.down` 硬编码 teams 列清单，须随上游新列同步。

（原 §9-9"v2.1.52 重做未收口"属适配状态，已移入 `02-v2.1.52适配矩阵.md` §8 并随 `d0beccbf8` 关闭。）

## 9. 机读 manifest 说明

`01-原版二开资产清单.manifest.json`（schema `aionui-2dev-asset-inventory/2`）登记全部 55 项资产（19 A 源文件 + 27 测试 + 12 B + 12 C 接点按 hostFile 归并 + D 级 4 组 + i18n 4 组），每项含 `grade/feature/sourceCommit/responsibility/hostDeps|anchors/contracts/triggers/migration/verification`，供后续抽包工具做符号核对与 slot 重放。结构为双层：`originalSource`（本文全部原版事实）+ `adapterTarget`（v2.1.52 适配状态映射，字段仅做 id→状态 映射，实体描述在 `02-v2.1.52适配矩阵.md`）。扩展包 manifest 字段的映射关系见 `03-扩展包与adapter决策.md` §3。
