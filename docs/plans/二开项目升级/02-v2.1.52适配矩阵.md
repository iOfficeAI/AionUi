# 02 - v2.1.52 适配矩阵（重做线状态与移植判定）

> 建立：2026-08-10 · P8-1 物理分层（自原 `01-二开代码资产与依赖分级.md` 的 v2.1.52 栏位析出，见 `legacy/`）
> 本文**只写 v2.1.52 重做线的适配事实**：提交对应、插入点、移植状态、迁移窗口、修复记录。原版资产定义与契约以 [`01-原版二开资产清单.md`](./01-原版二开资产清单.md) 为准；扩展包形态决策（DR-1）见 [`03-扩展包与adapter决策.md`](./03-扩展包与adapter决策.md)。
>
> 数据口径（新版宿主）：
> - `/private/tmp/aionui-latest-poc`（worktree）@ `bd728fb4d`，基线 AionUi v2.1.52（`7ebae30aa`）× AionCore v0.1.62，15 个重做提交（43 文件 +2476/−30）。
> - 临时团队 UI 组件与 SendBox 接线已于 2026-08-09 收口为 `d0beccbf8 feat(team): restore ad-hoc collaboration UI and runtime`（原"未提交工作区改动"状态失效，来源审计 §8）。
> - 专家团弹窗层级/对齐/重复加号 UI 回归已于 2026-08-10 修复为 `21d73ba45`（P7，见 §7）。
> - AionCore 迁移窗口证据：重做线 `a887fc23`（分支 `p2/v0162-adhoc-presets`），repair 脚本引入提交 `e0e03ab8`。

## 1. 提交对应（原版链 → v2.1.52 worktree）

| 功能 | 原版提交链（01 §1.2） | v2.1.52 worktree 对应 |
| --- | --- | --- |
| 临时团队 | `700fdf117` → `adf8dfaa4` → `8cbd1e833` → `ea7757ce5` | `c0fbf26ae`/`a1f3e4b0b`（桥接适配 v0.1.62）、`c630ab397`、`1a7d1082c`、`058fbbc15`、`ff079002a` |
| 专家团 | `e3f154559` → `ebca50b35` | `f5ab5f9b2`（TeamPresetPanel）、`16cceab84`（团队头预设卡片）、`9990640d6`（恢复创建流）、`bd728fb4d`（i18n） |
| ACP 团队能力覆盖 | `63ffc91e9` | `a1f3e4b0b` |
| 构建对齐 | `cc9f05549`、`b19a68072` | 随基线自带 |
| 本地 DB 迁移 | `700fdf117`（migration_v27） | v2.1.52 窗口在 AionCore 仓 038/039/040（`a887fc23` 线，§5） |

## 2. A 级资产移植状态

- **协议与类型（adHocTeamBridge/adHocTeamTypes/teamPresetBridge/types/useTeamPresets）**：**已移植**（P3 完成，随 `c0fbf26ae` 适配 v0.1.62）。
- **临时团队 UI 四组件**：**未移植**（git 内无 `components/AdHocTeam/` 目录；磁盘拷贝已随 `d0beccbf8` 收口归位）。审计 §4.1 判定 AgentSelectorModal/TeamStatusCard 可直接迁移（依赖的新版组件均在位），Launcher/Section 需按 §3 插入点适配。
- **hooks 与纯逻辑**：三纯逻辑模块未移植（审计 §4.1 判定可直接迁移，依赖的 chatLib/ipcBridge 新版均在）；hook 被重写为 27 行 SWR 精简版（无事件订阅/未读/运行态），审计 §4.2 判定**应以旧版全量形态替换**（规格以事件订阅为准，SWR 可保留仅作查询层）。
- **专家团 UI**：**已移植但形态变更**——平铺为 `pages/team/components/TeamPreset*.tsx` + 新增 `TeamPresetPanel.tsx`（`f5ab5f9b2`）+ 团队头预设卡片（`16cceab84`）；平铺形态作为扩展包基准是 adapter 决策（**DR-1**，见 `03-扩展包与adapter决策.md` §2），不改变 01 §2.4 的原版目录形态记录。

## 3. B 级插入点对照（旧版锚点 → v2.1.52 插入点/状态）

### 3.1 会话区（吸收审计 §1.2/§5）

| 宿主文件 | 旧版插入（01 §3.1） | v2.1.52 插入点（审计 §5） |
| --- | --- | --- |
| `ChatConversation.tsx` | aionrs `:209-228`；ACP `:374-393`；hook `:203/:284`；`:309` | `:262`（aionrs headerExtra）、`:439`（ACP/antigravity headerExtraNode），替换/包裹现有 `AdHocTeamHeaderAction`（`:148-197`）；保留 antigravity 复用 ACP 渲染面与 project workspace props |
| `platforms/acp/AcpChat.tsx` | props/解构/`:99` | 新版同位 |
| `platforms/acp/AcpSendBox.tsx` | `:695-696` | 新版 `:683-827` 区域接 `disabled={isTeamRunning}` |
| `platforms/aionrs/AionrsChat.tsx` | `:60-65` | 新版同位 |
| `platforms/aionrs/AionrsSendBox.tsx` | `:175`、`:214` | 新版 `teamRuntime?: TeamSendBoxRuntime` 插口（`:117-142`）+ prepareRuntime 切口 |
| `platforms/aionrs/useAionrsMessage.ts` | `:321-323` | 新版消息 switch 增 `teammate_message` case（**新版当前完全无此分支**）；ACP 侧上游已有 `:298/:437`，对齐归一化即可 |

### 3.2 团队页与侧栏、历史区

| 宿主文件 | 旧版插入（01 §3.2） | v2.1.52 状态 |
| --- | --- | --- |
| `pages/team/TeamPage.tsx` | `headerLeading:447-456` | **已移植**（`:530-537`），只需保真核对语义 |
| `Sider/SiderItem.tsx` | 类型 `:18`、菜单 `:113` | 待核对 |
| `GroupedHistory/ConversationRow.tsx` | `:236` | 待核对 |
| `GroupedHistory/types.ts` | `:60` | 待核对 |
| `GroupedHistory/ConversationSearchPopover.tsx` | `:196` | 待核对 |
| `GroupedHistory/hooks/useConversationListSync.ts` | `:143-152` | **待核对是否含等价过滤**（审计 §4.3：responseStream 全量注册防刷新循环的注释手法须保留） |
| `GroupedHistory/utils/groupingHelpers.ts` | `:92-125` | **已按 `1a7d1082c` 语义实现（`:93-105`），无需再迁** |

## 4. C 级改写的 v2.1.52 适配

| 宿主文件 | 原版改写（01 §4） | v2.1.52 适配 |
| --- | --- | --- |
| `pages/team/components/TeamCreateModal.tsx` | Tabs 双模式 + EditorModal 兄弟节点（最深改写） | 已由 `9990640d6` 重做创建流，直接对照 worktree 版本（平铺形态基准依据 DR-1，见 03 §2）；P7 回归修复 `21d73ba45`（§7） |
| `Sider/TeamSiderSection.tsx` | deletingTeamId 流 + isAdHoc 图标 + 删除回源 | **新版已有 ad-hoc 标记（`:123/:128`），旧版块不可重复插入**，只核对语义；P7 移除标题行重复 preset +（`21d73ba45`） |
| `platforms/acp/useAcpMessage.ts` | teammate 抽 hook + ensureStandalone 替换 | ACP 侧上游已有内联 `:298/:437`，重放"抽 hook+去重"改写 |
| `pages/settings/AgentSettings/InlineAgentEditor.tsx` | behavior_policy 逐字段构建 | 逐字段逻辑重插 |
| `hooks/agent/useAcpConfigOptions.ts` | `extra.teamId` → `[]` | 函数体重插 |
| `GroupedHistory/hooks/useConversationActions.ts` | deleteTeamSource* 强警告 | 条件分支重插 |
| `common/types/team/teamTypes.ts` | origin_conversation_id + preset 尾块 | 已在 worktree |
| `common/types/platform/acpTypes.ts` | behavior_policy 两字段 | 已由 `a1f3e4b0b` 对齐 v0.1.62 |
| `renderer/utils/model/agentTypes.ts` | BehaviorPolicy 镜像 | 同上 |
| `common/adapter/teamMapper.ts` | origin 透传 | 已在 worktree |
| `services/i18n/index.ts` | localStorage 守卫 | 可放弃或独立补丁 |
| `services/i18n/i18n-keys.d.ts` | +49 union | 部分已在 worktree（缺 collaboration.*，见 §6） |

## 5. D 级：迁移窗口（v2.1.52 重做线）

| 层 | v2.1.52 窗口事实 | 来源 |
| --- | --- | --- |
| AionCore 后端 DB | `crates/aionui-db/migrations/`：`038_ad_hoc_team_origin_conversation.sql`、`039_team_presets.sql`、`040_backfill_formal_team_leader_team_id.sql`（原版同名迁移 034/035/036 的平排；上游 v0.1.62 max=037）；`037_enable_grok_build_team` 已删（上游 `033_team_capability_criteria` 为超集） | 重做线 `a887fc23`（分支 `p2/v0162-adhoc-presets`） |
| 存量库修复 | `scripts/migration/repair-legacy-versions.sh`（checksum SHA-384 与 version 同事务）——**v0.1.62 重做线资产**，原版 PIN-CORE 无此脚本 | 引入提交 `e0e03ab8` |
| desktop 本地 DB | migration_v27 撞号风险与重编号规程沿用 01 §5.3/§5.4，窗口流程不变 | — |

> 编号历史：028→030→034/035/036（原版窗口）→038/039/040（v2.1.52 窗口）。两窗口编号不可混写（来源审计 §7.1）。

## 6. i18n 移植状态（49 key × 13 语言，清单见 01 §6）

| 组 | v2.1.52 状态 |
| --- | --- |
| `conversation.collaboration.*`（13） | **未移植**（新版只有 team.sider/team.presets 键） |
| `conversation.history.deleteTeamSource{Title,Confirm,Ok}`（3） | 待核对 |
| `team.presets.*`（31） | 已移植（`6316b28b6`/`bd728fb4d` 补齐） |
| `team.sider.adHocTooltip`、`team.sider.deleting`（2） | adHocTooltip 已移植 |

规格内缺口（`collaboration.running/runDetail` 仅中英、`createdSuccess` 未引用）为原版事实，见 01 §6；移植时原样或补齐由 leader 决策。

## 7. 移植判定汇总（吸收审计 §3/§4/§7）与 P7 修复记录

- **已移植（高保真）**：P3 协议层全量（两 bridge + 类型 + teamMapper + ipcBridge 接入 + 4 个 common 测试）、TeamPage 回源、TeamSiderSection ad-hoc 标记、groupingHelpers 可见性、TeamPresetPanel/Cards、team.sider/team.presets i18n。
- **部分移植（有形差）**：入口形态（新版 AdHocTeamHeaderAction 双按钮 vs 规格 Launcher→Modal→完整状态卡，**卡片信息密度与交互未还原**）；创建流程（新版直接 fromConversation 无 target、未走 AgentSelectorModal；失败提示复用 `team.sider.createTeam`，规格为 `conversation.collaboration.createFailed`）；hook（27 行 SWR 版 vs 规格 284 行全量事件订阅版）。
- **未移植**：AdHocTeam 四组件、useTeammateBackflow/runtimeGate/conversationTeamOwnership 三模块、aionrs 侧 teammate_message 回流、sendbox 发送分流与 isTeamRunning 禁用、`conversation.collaboration.*` i18n。
- **不可机械复制**（审计 §4.3）：旧版 ChatConversation 集成块（v2.1.52 已重构 antigravity `:124-128/:341-346/:397-400`、project `:228`、fork `:363/:98-144`）、AcpSendBox（新版 843 行结构）、TeamSiderSection/TeamPage（新版已含等价语义）、groupingHelpers（`1a7d1082c` 已实现）。
- **包边界与实施顺序**（审计 §7）：包 A-adhoc-ui 对外切口仅 5 处（ChatConversation 两挂载点、useAionrsMessage 一个 case、两 sendbox teamRuntime 插口接线、ipcBridge 两行 re-export、历史过滤[新版已含]）；顺序 ① i18n+三纯逻辑模块+四组件+旧 hook 全量 → ② 挂载点替换+ownership 接线 → ③ aionrs 回流+sendbox 分流/禁用 → ④ 旧 13 测试文件逐套点亮（每步独立提交+定向门禁）。

### P7 修复记录（2026-08-10，worktree 提交 `21d73ba45`）

专家团弹窗回归修复（undo 了 redo 引入的三处 UI 偏差，规格基准 = `e3f154559` + `设计规格归档/`）：

1. **弹窗层级根因**：redo 把 `<TeamPresetEditorModal>` 嵌为 TeamCreateModal `<AionModal>` 的 children，被困进父 wrap（zIndex 10000 + overflow:hidden）层叠上下文；修复为旧版 fragment 兄弟节点 + 编辑器 wrap 10001/mask 10000/autoFocus=false，关闭创建弹窗联动关编辑器。
2. **对齐**：编辑器按钮/输入/成员空态与 Picker 空态恢复旧版尺寸 token 与居中规则；presets pane 恢复 `min(54vh,470px)`/min390 与移动端堆叠。
3. **重复加号**：TeamSiderSection 标题行移除第二个 preset +（`team-preset-create-btn`），回归单一创建入口（e2e 无该 selector 引用，无破坏）。
   验证：focused vitest 29/29、oxfmt/oxlint/diff --check 干净、tsc 仅剩既有 js-yaml 报错。

## 8. 适配侧技术债状态

- 原 01 §9-9「v2.1.52 worktree 重做未收口」：**已关闭**——AdHocTeam 组件与 sendbox 接线于 2026-08-09 收口为 `d0beccbf8`（来源审计 §8）。
- 原版缺陷清单（disbanded 回航断链等 8 项）保留在 01 §8，移植时按 leader 决策处理。
