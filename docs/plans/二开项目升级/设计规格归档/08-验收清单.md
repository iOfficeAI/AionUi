# 08 · 验收清单

> 前端复现完成后逐项核对。每项标注规格出处（文档章节 + 源码文件）。
> 标准：**逐像素（尺寸/间距/圆角/颜色 token）+ 逐状态（状态矩阵 05 全绿）+ 逐文案（07 键位 13 语言一致）**。
> **性质声明**：以下全部为**待执行的验收要求**——未勾选即未验证，不应被解读为 e3 或 v2.1.52 已通过的验证结果。

## A. 团队创建弹窗

- [ ] A1 桌面宽 900、maxWidth `calc(100vw-72px)`；窄屏 `calc(100vw-32px)`；zIndex 10000/9999 — 02 §1
- [ ] A2 标题「新建团队」+ 副标题长文案 + 关闭钮 `button[aria-label="Close"]` — 02 §1
- [ ] A3 双栏各 1fr，body 高 `min(54vh,470px)`/min 390，中缝 `border-r border-border-3` 通栏 — 02 §3
- [ ] A4 Tabs「所有助手 (N) / 专家团」justify；左栏列表可滚动不塌缩（module.css 修复原样保留）— 02 §7
- [ ] A5 助手行 44px（modal 密度）：头像 30 rd-8 + 名 14/500 + 右侧 30×30 ＋/Spin 固定位 — 02 §3.1
- [ ] A6 禁用助手行灰字禁点 + Tooltip 原因（挂 body，zIndex 10060）— 02 §3.1
- [ ] A7 目录空 → 虚线框 min-h-112「未检测到可用的助手」；搜索无结果 → Empty「未找到结果」— 02 §3.1
- [ ] A8 「已选成员 N」15/600 + ⚑=Leader 图例 + 12px 辅助说明 — 02 §4
- [ ] A9 成员框 bg-fill-1 rd-8 min-h-120 滚动；空态两段居中文案（max-w-260）— 02 §4
- [ ] A10 成员行 40px：crown 26（选中 warning 填充+16%底）/ 移除 24；唯一 Leader、删除自动移交 — 02 §4
- [ ] A11 字段栅格 76px 标签列：团队名* input 38 + WorkspaceFolderSelect — 02 §5
- [ ] A12 确认创建 disabled 三连条件；校验 Message.warning 顺序 名称→成员→Leader — 02 §6.1
- [ ] A13 窄屏单栏：添加成员胶囊钮（h-30 rd-999）+ 下拉 w-260 z-10020 选中即关；成员框 max-h-38vh — 02 §8
- [ ] A14 关闭弹窗全部状态重置 — 02 §2

## B. 专家团

- [ ] B1 Picker 标题行「专家团」15/600 + primary mini「新建」；搜索框（旧版不过滤）— 01 §2
- [ ] B2 卡片：默认 fill-1/border-2 → hover border-primary/fill-2；选中 border-primary + primary-6/8% 底 — 01 §2.3
- [ ] B3 卡片整面 Button（键盘 Enter 可选中）；「调用/⋯」hover 淡入、stopPropagation — 01 §2.3
- [ ] B4 「⋯」菜单 编辑/删除；删除 Modal.confirm danger（含「不会影响已创建的团队」）— 01 §6
- [ ] B5 空列表：标题行保留 + 居中 emptyDescription — 01 §2.4
- [ ] B6 Preview：44px rd-12 图标 + 名 16/600 + 分类 Tag small；简介 13/20；tags arcoblue — 01 §3
- [ ] B7 无分类/无 tags/无示例 → 对应块不渲染 — 01 §3
- [ ] B8 缺失成员：警示条 danger-6/8%、成员红名 + red「缺失」Tag、调用钮 disabled — 01 §3/§4
- [ ] B9 成员行：22px 圆头像 + 13/500 名 + Leader orange Tag（Crown 12 + 队长）— 01 §4
- [ ] B10 未选中：居中「选择一个专家团以预览详情。」— 01 §3
- [ ] B11 调用回填：切 assistants Tab + 名称/成员（缺失跳过）/Leader 回退首位 — 02 §6.2
- [ ] B12 编辑器 720 宽、zIndex 10001、max-h-70vh；栅格 90px 标签列；名称*/成员*必填 — 01 §5
- [ ] B13 tags/示例 chips 去重、Enter 或「添加」提交、可删 — 01 §5.2/5.3
- [ ] B14 编辑器成员区：compact picker + 草稿框 max-h-240；编辑回填缺失成员「名字 (missing)」— 01 §5.4
- [ ] B15 保存校验三连 warning（名称/成员/Leader）+ 失败 error「保存专家团失败」— 01 §5.5
- [ ] B16 窄屏：Picker/Preview 上下堆叠 gap-16 — 01 §7

## C. 团队页与胶囊栏

- [ ] C1 标题行：临时团队有「←」返回源会话；成员>1 显示视图切换 — 03 §1/§3
- [ ] C2 胶囊 h-34 rd-999 bg-2；选中 = 身份色描边（底色不变）；名字身份色 13/600 — 03 §2.1
- [ ] C3 状态点 8px 五态（active 绿脉冲/灰/红/dormant 空心）；pending ‼️ wiggle — 03 §2.1
- [ ] C4 hover 增宽露手柄/✎/✕（Leader 无手柄无 ✕）；改名内联 input（Enter/Esc/blur）— 03 §2.1
- [ ] C5 拖拽仅水平、激活 8px、Leader 固定首位、dragActive 冻结他人 hover — 03 §2.1
- [ ] C6 溢出两端 28px 渐隐（阈值 10px）；「＋ 添加成员」固定最右、禁用仅文字变浅 — 03 §2.2
- [ ] C7 身份色板 8 色、Leader=0 品牌色、钉死+最小空档分配 — 03 §6
- [ ] C8 并行列 `flex 1 1 400px` min 400（≤2 人 240）、列间分隔线、列身 4% 身份色底 — 03 §4
- [ ] C9 列抬头 h-40：彩名 + 模型选择器（max-w-140）+ ⛶；无移除入口 — 03 §4
- [ ] C10 列溢出横滚 + snap + 两侧渐变箭头（32px 黑圆钮）；点胶囊滚动+闪动 — 03 §4
- [ ] C11 视图切换分段控件样式与持久化（按团队）— 03 §3
- [ ] C12 移除成员 Modal.confirm danger；active 成员强措辞 — 03 §9

## D. warmup 与空状态

- [ ] D1 遮罩 top=41px（露出列抬头）、bg-1 80% + blur(3px)、max-w-420 — 03 §7
- [ ] D2 头像 34px ≤6 个：idle .35 / pending .75+呼吸+55%色环 / ready 色环+外发光 / failed 灰化+红环+! — 03 §7.1
- [ ] D3 warming：标题+副标+180×4 扫动进度条（1.4s sweep）— 03 §7.2
- [ ] D4 error：标题四态（单/Leader/多/兜底）+ 错误框（单 64 高/多 120 高逐行）+ 精简原因 + 引导两态 + 重试钮 — 03 §7.3
- [ ] D5 warming 中胶囊操作全禁；失败后解禁且失败胶囊标红 — 03 §2.1/§7.3
- [ ] D6 对话空态：48 头像 + 彩名 + Leader/成员问候 + Leader 3+1 建议卡点击预填 — 03 §8

## E. 入口与临时团队

- [ ] E1 侧栏标签行「团队」+ chevron + ＋（team-create-btn）；展开状态记忆 — 04 §2.1
- [ ] E2 团队项图标三态（Peoples/Comment 临时/Spin 运行）；未读徽标 99+ hover 隐藏 — 04 §2
- [ ] E3 菜单 置顶/重命名/删除；删除确认 warning + 删除中禁用；重命名弹窗 rd-12 — 04 §2.1
- [ ] E4 收起态 40px 图标项 + Tooltip（临时追加「（临时团队）」）— 04 §2.2
- [ ] E5 会话抬头 👥 → AgentSelectorModal（600×400、loading Spin、回显条、提交中禁关、失败保窗）— 04 §3
- [ ] E6 TeamStatusCard 全要素：标题/状态点/解散标/运行标/成员数/未读数/预览 max-w-160/活跃轮次；解散点击回源 — 04 §3.3
- [ ] E7 添加成员下拉：标题+副标、行内 Spin、成功切 tab、「告诉 Leader →」预填草稿 — 04 §4

## F. 文案与工程

- [ ] F1 07 文档全部键 13 语言齐备；新增键过 `bun run i18n:types && node scripts/check-i18n.js` — 07
- [ ] F2 无硬编码色值（身份色板除外）；无新增原生交互元素（TeamTabs 既有例外原样保留）— README §复现总原则
- [ ] F3 全部 data-testid 与旧版同名（见附录）— 02 §9
- [ ] F4 旧版 DOM 测试可对等通过：TeamPresetPicker / TeamCreateModal.presets / useTeamPresets / TeamTabs / TeamWarmupOverlay — tests/unit/renderer/team/

## 附录 · data-testid 清单（规格的一部分）

| testid | 位置 |
| --- | --- |
| `team-create-btn` | 侧栏新建团队入口 |
| `team-create-modal` / `team-create-layout` / `team-create-layout-mobile` | 创建弹窗壳与布局根 |
| `team-create-assistant-pane` / `team-create-details-pane` | 左栏 / 右栏（窄屏下拉面板复用 assistant-pane） |
| `team-create-name-input` / `team-create-workspace-trigger` / `team-create-workspace-menu` | 团队字段 |
| `team-create-add-member-btn` | 窄屏添加成员锚钮 |
| `team-create-agent-search` / `team-create-agent-option-{id}` | 助手选择器（testIdPrefix 规则同） |
| `team-create-member-list-box` / `team-create-member-draft-{sid}` / `team-create-member-remove-{sid}` | 成员草稿 |
| `team-preset-picker` / `preset-picker-new` / `preset-picker-search` / `preset-picker-item-{id}` / `preset-picker-invoke-{id}` / `preset-picker-more-{id}` | 预设选择器 |
| `preset-empty-create` | 预设空态创建钮 |
| `team-preset-preview` / `preset-preview-name` / `preset-preview-description` / `preset-preview-invoke` | 预设预览 |
| `team-preset-editor-modal`（class）/ `preset-editor-name` / `preset-editor-category` / `preset-editor-description` / `preset-editor-tag-input` / `preset-editor-example-input` / `preset-editor-agent-*` | 预设编辑器 |
| `team-tab-bar` / `team-tab-{slot}` / `team-tab-name-{slot}` / `team-tab-status-{slot}` / `team-tab-drag-{slot}` / `team-tab-edit-{slot}` / `team-tab-remove-{slot}` / `team-tab-failed-{slot}` / `team-tab-add-member` | 胶囊栏 |
| `team-add-member-panel`（+`-header`/`-guidance`）/ `team-add-member-tell-leader` | 添加成员下拉 |
| `team-view-toggle` / `team-view-toggle-parallel` / `team-view-toggle-single` | 视图切换 |
| `team-warmup-overlay`（`data-phase`）/ `team-warmup-avatar-{slot}`（`data-status`）/ `team-warmup-error` / `team-warmup-retry` | warmup 遮罩 |
| `team-back-to-origin` | 团队页返回源会话 |
| `team-section-toggle` / `team-icon-{id}` / `team-spinner-{id}` / `collapsed-team-item-{id}` 等 | 侧栏团队区 |
| `collaboration-launcher-trigger` / `agent-selector-modal` / `agent-selector-loading` / `agent-selector-cancel` / `agent-selector-confirm` / `agent-selector-selected-name` | 临时团队发起 |
| `team-status-card`（+`-title`/`-name`/`-indicator`/`-disbanded-label`/`-running`/`-member-count`/`-unread-count`/`-message-preview`/`-run-detail`） | 团队状态卡 |
