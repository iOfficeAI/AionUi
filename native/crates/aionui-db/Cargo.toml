[package]
name = "aionui-db"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
serde_json = { version = "1", features = ["preserve_order"] }
thiserror = "2"
